function soma(n1=0, n2=0){ /* podemos definir o n1 e n2 como 0, para que se um dos valores não for informado a conta possa ser feita mesmo assim, caso contrário aparecia NaN na tela*/ 
    return n1 + n2
}

console.log(soma (2, 7)) // aqui a chamada está dentro do console.log